# Changelog

주요 변경과 문제 해결 기록을 남깁니다.

## [Unreleased]

### Added

- 토닥루틴도우미와 토닥루틴의 픽셀 아트 아이콘/스플래시/알림 아이콘 시안
- 토닥루틴도우미·토닥루틴 아이콘, 세로형 스플래시, 토닥루틴 흰색 단색 알림 아이콘 전달용 리소스 세트 추가 (`design-concepts/2026-07-22/todak-brand-resource-kit/`)
- 토닥루틴 알림용 흰색 단색 투명 PNG 512/192/96px
- 수유, 낮잠, 헬멧, 목욕, 투약 위젯용 흰색 단색 투명 PNG 아이콘 5종

### Changed

- 날짜가 바뀌면 기본 설정은 유지하고 오늘 스케줄과 실제 기록만 자동 초기화
- 앱 표시 브랜드를 `토닥루틴도우미`로 정리
- 도우미 앱 아이콘을 모빌 콘셉트로 교체
- 연관 아이콘 수정안에서 궤도를 제거하고 얼굴을 확대했으며, 오른쪽 위 같은 위치의 살구빛 달/금빛 별과 복숭아 크림/버터 크림 배경으로 두 앱을 구분
- 위젯 헬멧 아이콘을 얼굴 개방부와 아래 고정 부분이 보이는 옆모습 실루엣으로 개선
- 도우미 스플래시를 선택된 B안으로 교체
- 카드뉴스 문구를 `오늘 꼬인 루틴을 다시 맞추는 도우미` 포지션으로 재작성하고, 이미지 고정 문구 대신 화면에서 바로 수정 가능한 카드 UI로 변경
- 토닥루틴 알림 아이콘 원본을 512px 타이트 크롭과 흰색 단색 투명 PNG로 교체
- 토닥루틴 궤도 앱 아이콘 원본의 외부 흰 배경을 투명 처리하고 내부 흰 구름·반짝임은 유지
- GitHub Pages 기반 PWA 한계가 명확해져 도우미 앱에서 일정 알림 UI와 예약 코드를 제거
- 문서를 보존하기 위해 `build:pages` 출력 위치를 `docs/`에서 `dist-pages/`로 변경
- 카드뉴스 배경 시트에 그려진 말풍선 안에 텍스트가 들어가도록 카드 레이아웃(제목/본문/불릿 크기, 정렬)을 재조정
- 카드뉴스에서 화면 오버레이 텍스트를 제거하고 카드는 배경 이미지(말풍선에 글자를 직접 합성한 시트)만 표시하도록 변경. 문구 수정은 `public/brand/todak-cardnews-background-sheet.png` 교체로 처리
- 카드뉴스를 낱장 이미지(`public/brand/card-news/slide-1~6.png`) 방식으로 전환. 문구가 합성된 6장 이미지를 `<img>`로 순서대로 표시하며, 슬라이드 수정은 해당 파일 교체로 처리
- 이메일 수집 팝업을 테스터 모집 버전으로 교체: 제목/문구 변경, 테스터 신청 동의 체크박스(`tester_consent` 웹훅 필드), 문의 이메일 안내 추가
- 실제 방문자가 받는 큰 이미지(스플래시, 카드뉴스 6장)를 PNG → WebP로 변환(품질 88). 합쳐서 3.6MB → 317KB. PNG 원본은 `design-concepts/brand-archive/originals/`에 보관하고, `index.html`과 `cardNewsImageUrl`의 참조 확장자를 `.webp`로 변경. 작은 헤드라인 글씨까지 포함해 화질 차이를 눈으로 확인했다
- 앱이 쓰지 않는 브랜드 이미지 8개(11MB)를 `public/brand/`에서 `design-concepts/brand-archive/`로 이동. `public/`은 통째로 빌드 결과물에 복사돼 배포되므로, 안 쓰는 이미지는 배포본에서 빼되 저장소에는 남긴다. 배포 산출물이 16MB → 4.9MB로 줄었다
- 앱 아이콘 4종(`app-icon-512/192`, `icon-512/192`)에 캡처 때 딸려 들어간 **마우스 커서 얼룩 제거**. 카드 오른쪽 테두리 위에 걸쳐 있어서, 같은 x의 바로 위쪽 띠를 떠와 덮는 방식으로 테두리선이 끊기지 않게 했다
- 이메일 팝업과 카드뉴스 소개 문단에 **알림은 토닥루틴 앱에서 지원 / 안드로이드 우선 출시** 안내 추가. 이메일을 남기기 전에 보이는 자리에 넣었다
- 이메일 팝업에 **문의·바라는 점 한 줄 입력(선택)** 추가. 웹훅 필드 `message`로 함께 전송되며, 동의 문구의 수집 항목에도 "문의 내용(입력 시)"을 포함시켰다. **구글시트 쪽 Apps Script를 새 버전으로 다시 배포해야 이 값이 기록된다**(`GOOGLE_SHEETS_WAITLIST.md` 2번)
- 이메일 팝업에 **개인정보 수집·이용 동의(필수 체크박스)** 추가. 수집 항목/목적/보유 기간/거부 권리를 문구로 명시하고 개인정보처리방침을 링크했으며, 동의 전에는 제출 버튼이 눌리지 않는다. 테스터 동의 체크박스는 (선택)으로 표시하고 "구글 계정 이메일 필요" 안내를 덧붙였다
- 링크 공유 미리보기용 **OG/트위터 메타 태그**와 전용 이미지 `public/brand/og-cover.png`(1200x630) 추가. 생성 스크립트는 `scripts/make-og-cover.py`
- 웹훅 실패를 실패로 알리도록 이메일 제출 처리 수정 (PROB-007 참고)
- `npm audit fix`로 postcss 취약점(GHSA-r28c-9q8g-f849) 해소. 남은 esbuild/vite 권고는 **개발 서버에만 해당**하고 vite 8 메이저 업그레이드가 필요해 출시 후로 미룸
- 저장소 정리: `docs/`에 남아 있던 옛 Pages 빌드 산출물 14MB(`index.html`, `assets/`, `brand/`, `icons/`, `manifest.webmanifest`, `service-worker.js`, `refresh.html`) 삭제. 전부 `public/`과 바이트 동일한 사본이었고, CLAUDE.md의 "`docs/`는 문서 폴더" 규칙과도 어긋났다. `tsc -b`가 생성하는 `vite.config.js`/`vite.config.d.ts`도 추적 해제하고 `.gitignore`에 추가
- 깨시(깨어 있는 시간) 설정 추가: 낮잠 사이 깨시(공통/개별)와 막수 전 깨시를 설정에서 조절할 수 있게 하고, 낮잠 배치 계산과 막수 전 경고 카드가 이 값을 사용하도록 반영. 기본값은 공통 2시간·막수 전 2시간 30분으로 기존 동작과 동일

## 문제 해결 기록

### [PROB-007] 이메일 수집이 실패해도 화면엔 "등록됐어요"가 떴음 (2026-07-24)

**문제 (Problem)**: 이메일 팝업이 어떤 상황에서도 성공 문구를 띄웠다. 웹훅 주소가 빌드에 안 들어간 경우엔 개발자용 안내문("웹훅 주소를 넣으면...")이 성공 메시지 자리에 그대로 사용자에게 노출됐고, 그 상태에서는 이메일이 구글시트에 한 건도 안 쌓이는데 사용자도 운영자도 알아챌 방법이 없었다.

**원인 (Cause)**: 두 가지가 겹쳤다. ①`WAITLIST_WEBHOOK_URL`이 비어 있어도 `try` 블록을 그냥 통과해 성공 처리했다. ②구글 Apps Script 웹앱은 CORS 헤더를 안 주기 때문에 `mode: "no-cors"`로 보내야 하는데, 이 모드에서는 응답이 opaque라 성공/실패를 읽을 수 없다. 게다가 타임아웃이 없어 응답이 안 오면 버튼이 "저장 중..."에 계속 머물렀다.

**해결 방법 (Solution)**: 웹훅 주소가 없으면 성공이 아니라 **에러로 처리**하고 문의 메일 주소를 안내한다. 전송 전에 `navigator.onLine`을 확인하고, `AbortController`로 10초 타임아웃을 걸어 네트워크 문제는 `catch`로 잡히게 했다. 다만 `no-cors` 특성상 **서버가 500을 주는 경우까지는 여전히 구분할 수 없다** — 접수 건수는 구글시트에서 주기적으로 직접 확인해야 한다.

### [PROB-006] 빠른 기록으로 낮잠 종료 시 다음 낮잠 시간이 실제 종료를 반영하지 않음 (2026-07-16)

**문제 (Problem)**: 낮잠을 빠른 기록 버튼으로 시작/종료하면 실제 종료 시각을 입력했는데도 다음 낮잠 시간이 계산상 낮잠 길이를 기준으로 남을 수 있었다.

**원인 (Cause)**: 수동 시간 편집은 뒤 스케줄을 다시 계산했지만, 빠른 기록의 낮잠 종료 처리는 실제 기록만 저장하고 낮잠 스케줄 재계산 함수를 호출하지 않았다.

**해결 방법 (Solution)**: 빠른 기록으로 낮잠을 종료할 때도 실제 시작/종료 시각으로 낮잠 길이를 계산한 뒤, 같은 재계산 흐름을 적용해 뒤 낮잠 시간이 실제 종료 기준으로 갱신되도록 했다.

### [PROB-005] 다음 날에도 전날 루틴 수정값이 남음 (2026-07-15)

**문제 (Problem)**: 하루용 루틴 계산 도구인데, 전날 입력한 실제 기록과 시간 수정값이 다음 날에도 남을 수 있었다.

**원인 (Cause)**: 로컬 저장 데이터에 루틴 날짜를 구분하는 값이 없었다.

**해결 방법 (Solution)**: 저장 상태에 `routineDate`를 추가했다. 앱을 열었을 때 저장 날짜가 오늘과 다르면 기본 설정은 유지하고 스케줄과 실제 기록만 초기화하며, 앱을 켜둔 채 12시를 넘겨도 날짜 변경을 감지해 저장된 기준값으로 새 스케줄을 계산하도록 했다.

### [PROB-004] PWA 백그라운드 알림이 실기기에서 신뢰 불가 (2026-07-14)

**문제 (Problem)**: GitHub Pages에 배포된 토닥루틴도우미 PWA에서 앱 화면이 휴대폰에 떠 있지 않으면 일정 알림이 오지 않았다.

**원인 (Cause)**: 정적 PWA의 화면 타이머, Web Worker, Service Worker 타이머는 휴대폰 OS와 브라우저 백그라운드 정책에 의해 정지될 수 있다. 정해진 시각에 안정적으로 알림을 보내려면 네이티브 앱 로컬 알림 또는 서버 기반 Web Push가 필요하다.

**해결 방법 (Solution)**: 도우미 PWA에서 알림 권한 요청, 일정 알림 등록, 알림 테스트 UI와 관련 예약 코드를 제거했다. 알림은 토닥루틴 Flutter 본 앱 또는 서버 기반 Web Push 단계로 분리한다.

### [PROB-001] 화면을 띄워 둔 경우에만 일정 알림 수신 (2026-07-14)

**문제 (Problem)**: 앱 화면이 보이는 동안에는 일정 알림이 오지만, 홈 화면으로 내리거나 다른 앱을 보고 있으면 알림이 오지 않거나 크게 늦어졌다.

**원인 (Cause)**: 일정 예약이 브라우저 화면의 `window.setTimeout`에만 의존했다. 모바일 브라우저는 숨겨진 화면의 타이머를 제한하고, OS 절전 정책에 따라 앱 프로세스를 멈출 수 있다.

**해결 방법 (Solution)**: Web Worker와 Service Worker 기반 보조 예약을 시도했지만 실기기에서 신뢰성이 충분하지 않았다. 최종적으로 PROB-004에 따라 도우미 PWA의 알림 기능을 제거했다.

### [PROB-002] 알림 등록 후 일정 입력값이 바뀌는 문제 (2026-07-14)

**문제 (Problem)**: 첫기상, 첫수, 낮잠 길이 등을 나중에 수정할 수 있어 알림 등록 시점을 사용자가 판단하기 어려웠다.

**원인 (Cause)**: 수동 등록 버튼 문구 때문에 알림 시간이 최초 등록값에 고정되는 것처럼 보였다.

**해결 방법 (Solution)**: 이후 실기기 검증 결과에 따라 도우미 PWA의 알림 기능을 제거했다. 수동 등록 버튼과 관련 설정도 함께 삭제했다.

### [PROB-003] 알림 아이콘이 미리보기 배경을 포함함 (2026-07-14)

**문제 (Problem)**: 토닥루틴 알림 아이콘 시안이 어두운 미리보기 배경을 포함해 실제 앱 자산으로 바로 사용할 수 없었다.

**원인 (Cause)**: 디자인 확인용 이미지와 Android 알림용 단색 알파 자산이 분리되지 않았다.

**해결 방법 (Solution)**: 선택된 아기 얼굴, 머리 꼭대기 C자 컬, 궤도 실루엣은 유지하면서 흰색 단색 + 완전 투명 배경 PNG로 변환했다. 512px 마스터와 192px, 96px 파생 파일의 알파 채널과 RGB 값을 검증했다.
